# ===========================================
# Otter Money Environment Variables
# ===========================================
# Copy this file to .env and fill in the values

# Database (PostgreSQL)
# For local dev, use your local PostgreSQL instance
# For production, use a managed service (Supabase, Neon, RDS, etc.)
DATABASE_URL="postgresql://user:password@localhost:5432/otter_money?schema=public"

# JWT Secret (generate a random string: openssl rand -base64 32)
JWT_SECRET="your-super-secret-jwt-key-change-this"

# App URL (used for invite links, etc.)
APP_URL="http://localhost:3000"

# CORS Origin (frontend URL)
CORS_ORIGIN="http://localhost:3000"

# Redis (for caching and job queue)
REDIS_URL="redis://localhost:6379"

# ===========================================
# Plaid Integration (https://dashboard.plaid.com)
# ===========================================
PLAID_CLIENT_ID="your-plaid-client-id"
PLAID_SECRET="your-plaid-secret"
# Options: sandbox, development, production
PLAID_ENV="sandbox"

# ===========================================
# SimpleFin Bridge (https://beta-bridge.simplefin.org)
# ===========================================
# SIMPLEFIN_SETUP_TOKEN="your-simplefin-setup-token"

# ===========================================
# Claude API (for Wally AI assistant)
# ===========================================
ANTHROPIC_API_KEY="your-anthropic-api-key"

# ===========================================
# AWS SES (for password reset, notifications)
# ===========================================
# Create IAM user with SES permissions, get credentials
AWS_ACCESS_KEY_ID="your-aws-access-key-id"
AWS_SECRET_ACCESS_KEY="your-aws-secret-access-key"
AWS_REGION="us-east-1"
EMAIL_FROM="no-reply@otter.money"
